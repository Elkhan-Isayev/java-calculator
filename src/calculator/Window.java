package calculator;

import javax.swing.*;

public class Window extends JFrame {
    public Window() {
        //  Set empty layout manager
        setLayout(null);
        setSize(365, 485);
        setVisible(true);
        //  Set spawn location
        setLocation(100, 100);
    }
}
